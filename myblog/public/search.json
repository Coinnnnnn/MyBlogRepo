[{"path":"/:C++刷题笔记/","content":"数据结构两数之和给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出 和为目标值 target 的那 两个 整数，并返回它们的数组下标。 你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。 你可以按任意顺序返回答案。 示例 1： 输入：nums = [2,7,11,15], target = 9输出：[0,1]解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。 示例 2： 输入：nums = [3,2,4], target = 6输出：[1,2] 示例 3： 输入：nums = [3,3], target = 6输出：[0,1] 普通法：时间复杂度O（n^2),空间复杂度O(1)class Solution public: vectorint twoSum(vectorint nums, int target) int len = nums.size(); int i,j; for(i = 0;ilen;i++) for(j = i+1;jlen;j++) if(nums[i] + nums[j] == target) return i,j; return; //为引用的意思，起别名 ; 哈希查找表法时间复杂度O（1）class Solution public: vectorint twoSum(vectorint nums, int target) unordered_mapint,int hashtable;//定义一个unordered_map,它是一个容器类,用于存储元素集合，它不保障元素的排序但是提供更快的查找，插入和删除操作 for(int i = 0;inums.size();i++) auto it = hashtable.find(target - nums[i]);//unordered_map.find()提供一种返回方法，返回的是元素的iterator，hashtable.begin指向第一个元素的iterator，end则是一个不存在的元素位置。 if(it != hashtable.end())//it查找的是一整个元素，it-first查找的是元素的key，-second查找的是元素的值 returnit-second,i; hashtable[nums[i]] = i; return; //为引用的意思，起别名 ; 两数相加（链表）给你两个 非空 的链表，表示两个非负的整数。它们每位数字都是按照 逆序 的方式存储的，并且每个节点只能存储 一位 数字。 请你将两个数相加，并以相同形式返回一个表示和的链表。 你可以假设除了数字 0 之外，这两个数都不会以 0 开头。 链表/** * Definition for singly-linked list. * struct ListNode * int val; * ListNode *next; * ListNode() : val(0), next(nullptr) * ListNode(int x) : val(x), next(nullptr) * ListNode(int x, ListNode *next) : val(x), next(next) * ; */class Solution public: ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) //初始化链表头指针和进位 ListNode *head = nullptr; ListNode *p = nullptr; int carry = 0; //遍历链表直达两个链表都处理完 while(l1 || l2) //取当前节点的值，直到两个链表都处理完 int n1 = l1?l1-val: 0; int n2 = l2?l2-val: 0; int sum = n1 + n2 + carry; if(!head) head = p = new ListNode(sum % 10);//初始化头节点 else p-next = new ListNode(sum % 10);//链接后续节点 p = p - next; carry = sum/10; if(l1) l1 = l1 - next; if(l2) l2 = l2 - next; if(carry 0) p-next = new ListNode(carry); return head; ; 数据结构数据结构各个特点顺序表：数组，链表，栈，队列 栈：后进先出 队列：先进先出 树：二叉树 两种不同的定义方式使用void方法初始化（栈内存自动管理）使用void方法初始化，调用初始化方法时则需要传入地址 初始化方法内也不需要手动开辟新空间 typedef struct Queue\tElemType data[MAXSIZE];\tint front;\tint rear;\tQueue;//初始化void initQueue(Queue* Q)\tQ-front = 0;\tQ-rear = 0;//入队int equeue(Queue* Q, ElemType e)\tif (Q-rear = MAXSIZE !queueFull(Q)) return 0; Q-data[Q-rear] = e;\tQ-rear++;\treturn 1;//出队ElemType dequeue(Queue* Q)\tif (Q-front == Q-rear) printf(空的 ); return 0; ElemType e = Q-data[Q-front];\tQ-front++;\treturn e;int main(int argc, char const* argv[])\tQueue q;\tinitQueue(q);\tequeue(q, 10);\tequeue(q, 20);\tequeue(q, 30);\tequeue(q, 40);\tequeue(q, 50);\tprintf(%d , dequeue(q));\tprintf(%d , dequeue(q));\treturn 0; 使用结构定义初始化（堆内存手动管理）使用这种定义方法则无需在使用时传入地址 但是在初始化方法内需要手动开辟空间，并且return 该节点； #include stdio.h#include stdlib.h#define MAXSIZE 100typedef int ElemType;typedef struct\tElemType* data;\tint front;\tint rear;Queue;//初始化Queue* initQueue()\tQueue* q = (Queue*)malloc(sizeof(Queue));\tq-data = (ElemType*)malloc(sizeof(ElemType) * MAXSIZE);\tq-front = 0;\tq-rear = 0;\treturn q;//判断队列是否为空int isEmpty(Queue* Q)\tif (Q-front == Q-rear) printf(空的 ); return 1; else return 0;\t//出队ElemType dequeue(Queue* Q)\tif (Q-front == Q-rear) printf(空的 ); return 0; ElemType e = Q-data[Q-front];\tQ-front++;\treturn e;//队尾满了调整呢队列int queueFull(Queue* Q)\tif (Q-front 0) int step = Q-front; for (int i = Q-front; i = Q-rear; ++i) Q-data[i - step] = Q-data[i]; Q-front = 0; Q-rear = Q-rear - step; return 1; else printf(真的满了 ); return 0;\t//入队int equeue(Queue* Q, ElemType e)\tif (Q-rear = MAXSIZE) return 0; Q-data[Q-rear] = e;\tQ-rear++;\treturn 1;//获取队头元素int getHead(Queue* Q, ElemType *e)\tif (Q-front == Q-rear) printf(空的 ); return 0; *e = Q-data[Q-front];\treturn 1;int main(int argc, char const* argv[])\tQueue* q = initQueue();\tequeue(q, 10);\tequeue(q, 20);\tequeue(q, 30);\tequeue(q, 40);\tequeue(q, 50);\tprintf(%d , dequeue(q));\tprintf(%d , dequeue(q));\tElemType e;\tgetHead(q, e);\tprintf(%d , e);\treturn 0; 循环队列判断是否为满这里应该选A 递归概念就相当于找到an 的表达式，如斐波那契数列的表达式就是a【n】 a【n-1】+a【n-2】 ✅ 递归的本质是： 一个函数调用自己，并向着一个明确的结束条件前进。 树完全二叉树 前序遍历 中序遍历 后序遍历 二叉树的遍历性质已知前后序遍历不能唯一确定一棵二叉树 线索二叉树 树转换为二叉树 二叉树转换为树 森林与二叉树的转换 二叉树转森林 克鲁斯卡尔和普瑞姆算法总结 关键路径"},{"title":"Hello World","path":"/:hello-world/","content":"Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub. Quick StartCreate a new post$ hexo new My New Post More info: Writing Run server$ hexo server More info: Server Generate static files$ hexo generate More info: Generating Deploy to remote sites$ hexo deploy More info: Deployment"},{"title":"博客标题及封面测试","path":"/:测试博客上传/","content":"测试博客上传 测试封面图片第三次 这是本地路径图片 以上是经过picgo上传的图片","tags":["Unity","C#","游戏开发"],"categories":["技术"]}]